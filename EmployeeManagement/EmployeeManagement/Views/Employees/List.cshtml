@model IEnumerable<EmployeeManagement.Models.EmployeeListViewModel>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>List</title>
    <link href="~/Content/bootstrap.css" rel="stylesheet" />
</head>
<body>
    <div class="container">
        <div class="row">
            <div class="col-md-12">
                <h1>Employees</h1>
                <p>@Html.ActionLink("Add", "Add", "Employees") a new employee</p>
            </div>
        </div>
        <div class="row">
            <div class="col-md-12">
                <table class="table table-striped">
                    <thead>
                        <tr>
                            <th>Name</th>
                            <th>Phone</th>
                            <th>Department</th>
                            <th></th>
                            <th></th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var employee in Model)
                        {
                            <tr>
                                <td>@employee.Name</td>
                                <td>@employee.Phone</td>
                                <td>@employee.Department</td>
                                <td>
                                    @Html.ActionLink("edit", "Edit", "Employees", new { id = employee.EmployeeId }, new { @class = "btn btn-info" })
                                </td>
                                <td>
                                    @using (Html.BeginForm("Delete", "Employees", new { id = employee.EmployeeId }, FormMethod.Post))
                                    {
                                        <button class="btn btn-danger" onclick="return confirm('Are you sure you want to delete @employee.Name?');">Delete</button>
                                    }
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
    <script src="~/Scripts/jquery-3.3.1.js"></script>
    <script src="~/Scripts/bootstrap.js"></script>
</body>
</html>
